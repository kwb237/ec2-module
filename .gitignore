#Local .tfstate files,
*.tfstate
.tfstate.

#Crash log files,
crash.log

#Terraform variable files with sensitive data,
*.tfvars
*.tfvars.json

#Terraform plan output,
*.plan

#Sensitive override files,
override.tf
override.tf.json
*_override.tf
*_override.tf.json

#Terraform provider and module caches (if present),
**/.terraform/*
*.terraform.lock.hcl